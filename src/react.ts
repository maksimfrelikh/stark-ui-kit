// React entry — thin hooks over the framework-agnostic utilities in './index'.
// Import as 'stark-ui-kit/react'. Requires react >= 18 (an optional peer dependency).
export { useFocusTrap } from './lib/useFocusTrap';
export { useScrollLock } from './lib/useScrollLock';
